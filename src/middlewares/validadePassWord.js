export default function Validatepassword(password, confirmPassword) {
  if (password !== confirmPassword) {
    alert(
      "a senha de confimação deve ser igual a senha normal (arrumar essa frase dps kkkkkkkk)"
    );
  }
}
